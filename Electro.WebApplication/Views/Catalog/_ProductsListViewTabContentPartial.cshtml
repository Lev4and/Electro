@model List<Product>
<ul class="d-block list-unstyled products-group prodcut-list-view-small">
    @foreach (var product in Model)
    {
        @await Html.PartialAsync("_ProductRowPartial", product)
    }
</ul>